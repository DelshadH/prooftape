name: ProofTape
description: >-
  Pre-release placeholder. The released action will compare observed dependency
  behavior without running pull-request code in a privileged workflow.
inputs:
  dependency:
    description: Exact npm package name to observe
    required: true
  command:
    description: Existing test command
    required: true
runs:
  using: composite
  steps:
    - shell: bash
      run: |
        echo "ProofTape Action is not available before the 0.1 release." >&2
        exit 4
