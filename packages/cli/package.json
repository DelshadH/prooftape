{
  "name": "prooftape",
  "version": "0.0.0",
  "description": "Compare observed npm dependency behavior across exact Git revisions",
  "type": "module",
  "bin": {
    "prooftape": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=22.15.0"
  },
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DelshadH/prooftape.git",
    "directory": "packages/cli"
  },
  "dependencies": {
    "@prooftape/core": "0.0.0",
    "@prooftape/hook": "0.0.0",
    "@prooftape/schema": "0.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  }
}
